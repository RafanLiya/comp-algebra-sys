import AlgebraGrapher from '@/components/AlgebraGrapher';

export default function Home() {
  return (
    <main className="min-h-screen bg-gray-50 flex items-center justify-center">
      <AlgebraGrapher />
    </main>
  );
}
